"""Shared utilities for micro-army services.

Available modules:
    secrets     — Fetch secrets from AWS Secrets Manager with caching
    log_config  — Structured JSON logging for Loki/ELK ingestion
    metrics     — Prometheus metrics middleware + /metrics endpoint
"""
